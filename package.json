{
  "name": "gatekeeper-api-v2.0.0",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "build": "rimraf ./dist && npx tsc -p tsconfig.json && npx tsc-alias -p tsconfig.json",
    "dev": "nodemon -r tsconfig-paths/register ./src/app.ts",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "type": "module",
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "nodemon": "^3.1.7",
    "rimraf": "^6.0.1",
    "ts-node": "^10.9.2",
    "tsc-alias": "^1.8.10",
    "tsconfig-paths": "^4.2.0",
    "typescript": "^5.6.2"
  }
}
